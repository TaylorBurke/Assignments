function thoughtPolice() {

var story = "1984";
    var boy = {
         name = "Winston",
         isPerson = "True"
    }
    var girl = {
         name = "Julia",
         isPerson = "True"
    }

    // var miniTru is set to falsey but changes to truthy if var thoughtCriminal is true
var ministryOfTruth = 0;
var thoughtCriminal = true;

    while(thoughtCriminal = true) {
    ministryOfTruth += 1
    }

    if(ministryOfTruth = true) {
    console.log("Welcome to Room 101")
    }
}

thoughtPolice();